---
description: 仅设计阶段 — 执行 full-dev 阶段 1-2，产出计划后停下（供跨工具交接）
---

# /xdev:full-dev-design — 设计阶段入口（薄壳）

> 本文件不再单独维护流程。执行 **`full-dev.md` 的阶段 1（设计）+ 阶段 2（计划与门）**，
> 到"写状态文件 + commit 计划"为止停下，不要进入阶段 3。
>
> 用途：用强推理模型做规划，把实现交给另一个模型/工具——
> 对方以 `/xdev:full-dev-impl` 从状态文件 `docs/state/xdev--<branch>.md` 续跑。
>
> 阶段 1-2 的全部规则（规模自适应、3 反思、`--menxia` 门下门、状态文件格式）
> 以 `full-dev.md` 为唯一权威，本文件不重复。
